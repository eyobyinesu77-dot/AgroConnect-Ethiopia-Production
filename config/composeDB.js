// ComposeDB or external third-party service configuration setup
const composeConfig = {
  appName: 'AgroConnect Ethiopia',
  version: '1.0.0',
  environment: process.env.NODE_ENV || 'development',
};

module.exports = composeConfig;